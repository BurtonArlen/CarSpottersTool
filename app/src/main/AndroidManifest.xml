<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guest.carspotterstool">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.auth.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.user.ProfileActivity" />
        <activity android:name=".ui.contribution.AddEntryActivity" />
        <activity android:name=".ui.contribution.ContributeActivity" />
        <activity android:name=".ui.appflow.MainActivity" />
        <activity android:name=".ui.contribution.SearchActivity" />
        <activity android:name=".ui.contribution.ContributionDetailActivity" />
        <activity android:name=".ui.user.IntentUserProfile" />
        <activity android:name=".ui.auth.SetUserInfoActivity"></activity>
    </application>

</manifest>